#include <stdio.h>  // Biblioteca padrão para entrada e saída de dados

int main() {
    // Declaração e inicialização de variáveis de diferentes tipos
    int idade = 5;            // Variável inteira, armazenando um número inteiro
    float altura = 1.32;      // Variável de ponto flutuante, armazenando um valor decimal de precisão simples
    double salario = 3000.50; // Variável de ponto flutuante de dupla precisão, usada para valores decimais maiores
    char opcao = 'S';         // Variável do tipo caractere, armazenando um único caractere

    return 0;  // Indica que o programa terminou com sucesso
}
